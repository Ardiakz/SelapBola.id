const fs = require('fs')
const path = require('path')
const dbPath = path.join(__dirname, '../migrations/20231027090611_products.js')

const productController = {
    getAll : async (req, res) => {
        try {
        
        } catch (error) {
            
        }
    }
}

module.exports = productController